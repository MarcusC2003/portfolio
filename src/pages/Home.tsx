import React from "react";
import Window from "../components/Window";

const Home: React.FC = () => {
  return (
    <Window title="Home">
      <h1 style={{ fontSize: "2rem", margin: 0 }}>home</h1>
    </Window>
  );
};

export default Home;
